/* Used to list the data types in the store and the set of actions that
can be performed on them */

export const DataTypes = {
  PRODUCTS: 'products',
  CATEGORIES: 'categories',
};

export const ActionTypes = {
  DATA_LOAD: 'data_load',
};
